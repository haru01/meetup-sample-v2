# AI駆動開発 1日体験研修

**対象**: 開発経験あり・AIエージェント初体験の開発者
**前提ツール**: Claude Code（インストール済み）、Node.js、GitHub CLI（`gh`）
**使用リポジトリ**: `meetup-sample-v2`（ミートアップサイト付きのコミュニティ管理API）

---

## この研修で目指すこと

「AIに相談できた」「AIにコードを書かせた」体験から一歩踏み込み、**エージェントにゴールを示して仕事を依頼できる**や、エージェントが自律的に動ける環境を設計の必要性を理解した開発者になる第一歩を踏む。

今日の体験を通じて、「楽しかった」で終わらせず、明日から自分のプロジェクトで実践できる具体的な手がかりを手に入れる。

---

## 学習目標ルーブリック

研修前に自己チェックし、終了後にもう一度確認してください。

| 観点 | Lv1：理解できる | Lv2：説明できる | Lv3：自分のプロジェクトに適用できる |
|------|---------------|---------------|----------------------------------|
| **1. AIエージェント理解** | チャット型とエージェント型の動き方の違いを答えられる | 「ゴールを示す→自律実行→レビュー」の流れと、チャット型との違いをチームメンバーに説明できる | 自分のタスクをエージェントに委譲するためのゴール定義文を書ける |
| **2. Claude Code CLI（プラン駆動）** | プランモードで計画を立ててから実装・検証する流れを説明できる | 計画 → 実装 → 品質ゲートの各ステップで何が起きているか説明できる | プランモードで計画を立て、実装・検証まで1つのIssueを完了できる |
| **3. 計画レビュー** | プラン提示の中身を読み、何が決まっていて何が抜けているか指摘できる | 受け入れ条件・参考コード・規約に照らして計画の妥当性を評価できる | 自分のIssueで計画の改訂を依頼し、合意してから実装に進められる |
| **4. コードレビューとずれの特定** | 生成コードと受け入れ条件・既存規約の差を読める | 規約違反・テスト不足・ドメイン理解のずれを言語化できる | ずれの根本原因を仮説立て、再実行で検証できる |
| **5. ハーネス改善サイクル** | ハーネスの構成要素（CLAUDE.md / conventions / hooks 等）を列挙できる | 観察したずれを CLAUDE.md・docs/conventions・.claude/hooks への変更案に落とせる | 改善前後の差を観察し、自プロジェクトのハーネスを継続的に育てられる |

---

## 1日のタイムライン

```text
09:15 - 09:45  オープニング
09:45 - 10:05  基礎知識：AIエージェントとは何か
10:05 - 10:45  ソースコードリーディング：AIに設計指針を解説させる
10:45 - 11:30  実践①：Claude Code CLIでチケットベース開発を体感する
11:30 - 12:00  学びの整理①

12:00 - 13:00  昼食

13:00 - 13:15  ワーク導入：3つの勘所と進め方
13:15 - 14:15  ラウンド1：計画レビュー → 実装・コードレビュー → ずれの抽出
14:15 - 14:30  休憩
14:30 - 15:30  ラウンド2：改善したハーネスで再実行・比較
15:30 - 15:45  発表準備
15:45 - 16:00  休憩
16:00 - 16:30  発表セッション：ペアごとに4分で発表
16:30 - 17:30  全体ふりかえり：これからの開発の未来を語り合う
17:30 - 17:45  クロージング：3行コミットメント＆感想
```

---

## 09:15 - 09:45 オープニング

### ゴール

- 1日の流れを把握する
- 「今日終わりに何ができるようになっていたいか」を言葉にする

### チェックイン

0. 付箋にかきだし
1. なまえ：普段のAIのつかいかた
2. 「今日1つだけ持ち帰るとしたら何か」を付箋に書く

---

## 09:45 - 10:05 基礎知識：AIエージェントとは何か（20分）

### チャット型との違い

まず、普段使っているチャット型AIとエージェント型AIの違いを押さえる。

| | チャット型（例：Claude.ai） | エージェント型（例：Claude Code） |
|--|--------------------------|---------------------------------------|
| **やりとり** | 1問1答 | ゴール提示 → 計画レビュー → 自律実行 → 作成物レビュー |
| **文脈の保持** | 人間がスレッドを管理 | ファイル（CLAUDE.md 等）が文脈を保持 |
| **ツール利用** | なし〜限定的 | ファイル読み書き・コマンド実行・ブラウザ操作など |
| **得意なこと** | 質問への回答、アイデア出し | ゴールに向かって試行錯誤し達成する |

> **一言で言うと**: チャット型は「聞けば答える」、エージェント型は「任せれば動く」。

### エージェントへの仕事の依頼の仕方

エージェントは「作業指示」を受けて動くのではなく、**ゴールを渡されると自律的に考えて動く**。
うまく依頼できると、エージェントは既存コードを調査し、計画を立て、実装し、検証まで一気に進める。

依頼に含めるべき要素は4つ：

| 要素 | 問い | 役割 |
| --- | --- | --- |
| **ゴール** | 何が達成されていればOKか？ | エージェントが向かう方向を決める |
| **制約** | 守らなければならないルールは何か？ | 既存設計・パターンから逸脱しないようにする |
| **具体例** | どんな状態になっていれば合格か？ | 実装のあいまいさをなくす |
| **検証手段** | どうすれば正しいと確認できるか？ | エージェントが自律的に合否を判断できるようにする |

**ゴール** — 「何をするか」ではなく「何が達成されていればOKか」を伝える

```text
✕ 「EventControllerにPATCHエンドポイントを追加して、Prismaでstatusを更新して返して」
○ 「主催者が DRAFT のイベントを PUBLISHED に公開できるAPIを実装して」
```

「何をするか」を指示すると、エージェントは言われた通りにしか動かない。
「何が達成されていればOKか」を伝えると、エージェントは既存コードを調べ、最善の手段を**自律的に**選ぶ。

**制約** — プロジェクトのルール・設計方針を渡す

エージェントは何も言わなければ「一般的な実装」をしようとする。
プロジェクト固有のパターン（Result型・Command構造・エラー設計など）は**ハーネスか参考ファイルで示す**。

```text
参考
- create-event.command.ts が実装パターンの参考になる（同コンテキストの実装済みコマンド）
- approve-member.command.ts がステータス遷移の参考になる（PENDING → ACTIVE）
```

**具体例（受け入れ条件）** — 「合格の定義」を箇条書きで渡す

受け入れ条件があると、エージェントはそれをテストケースとして使い、実装の精度が上がる。

```text
受け入れ条件
- [ ] `status=DRAFT` のときのみ公開できる
- [ ] すでに公開済みの場合 `EventAlreadyPublishedError` を返す
- [ ] 公開後は `status=PUBLISHED` に遷移する
- [ ] `EventPublished` が発火される
```

**検証手段** — エージェントが自律的に合否を判断できるようにする

「実装して」だけだと、エージェントはコードを書いて終わりにしがちになる。
「テストを通してから報告して」と伝えると、エージェントは自分で検証し、失敗したら修正して再試行するループを**自律的に**回す。

```text
品質ゲートを通してから完了報告して：
  cd backend && npm run test:coverage
  cd backend && npm run review
```

### ハーネス：エージェントが期待通りに動く環境

エージェントに「ゴールを示す」だけでは不十分なことがある。
プロジェクトのルール・構造・品質基準などを**ファイルとして整備**しておくことで、エージェントが自律的に正しく動ける。

この「エージェントが期待通りに動くための環境整備」を**ハーネス**と呼ぶ。

```text
ハーネスの例：
- CLAUDE.md,AGENTS.md → 技術スタック・ディレクトリ構造・命名規約・実行コマンド・品質基準などを宣言
- docs/conventions/   → テストガイドライン・実装パターン
- docs/decision-logs/ → 設計上の意思決定記録（ADR）
- .claude/settings.json → Claude Code の権限・hook・環境変数を宣言（Edit/Write 後に ESLint 自動実行など）
- テスト・Lint         → 品質の自動チェック
```

> **今日のポイント**: ハーネスの詳細は午後に体感する。まずは「エージェントにゴールを渡して動かす」体験をしよう。

---

## 10:05 - 10:45 ソースコードリーディング：AIに設計指針を解説させる（40分）

### ねらい

実践に入る前に、**AIエージェントにこのプロジェクトの設計と実装パターンを解説させる**。
自分で全ファイルを読むのではなく、「AIに聞く」ことでプロジェクトの全体像を素早く掴む。同時に、AIが**コードを読んで正確に説明できる**ことも体感する。

> **問い**: 初めてのプロジェクトに入ったとき、AIに何を聞けば効率的にキャッチアップできるか？

### 進め方

リポジトリルートで Claude Code を起動し、以下のプロンプトを順番にAIに投げる。
AIの回答を見ながら、参加者同士で「なるほど」「ここはどういう意味？」と対話する。

```bash
cd meetup-sample-v2
claude
```

### Step 1：event コンテキストの全体像を聞く（10分）

今日実装する event コンテキストの構造を把握する：

```text
backend/src/event/ のコードを読んで、このコンテキストの全体像を解説して。
- ディレクトリ構造とレイヤー構造
- 各レイヤーの依存方向を図で示して
- 今日これから実装する PublishEvent は、どこに何を追加すればよいか
```

**AIの回答で確認すべきポイント**:

- `controllers/` → `usecases/commands/` → `models/` の依存方向（外から内へ）
- `shared/` の Result型・エラー型を共通で使っている
- `CreateEvent` が実装済みで、`PublishEvent` は未実装であることを把握する

> **ファシリテーターヒント**: AIが依存方向の図を出したら、「なぜこの方向？逆だと何が困る？」と問いかけると理解が深まる。

### Step 2：実装済みの Command を題材に実装パターンを学ぶ（15分）

今日の課題と同じ event コンテキストの実装パターンを理解する：

```text
backend/src/event/usecases/commands/create-event.command.ts を起点に、
「主催者がイベントを作成する」シナリオの実装パターンを呼び出し順に解説して。
Result 型・エラー型・イベント発行のパターンも合わせて教えて
```

次に、ステータス遷移の参考として community コンテキストを見る：

```text
backend/src/community/usecases/commands/approve-member.command.ts を読んで、
ステータス遷移（PENDING → ACTIVE）の実装パターンを解説して。
今日実装する PublishEvent（DRAFT → PUBLISHED）はこのパターンに近い。
どこが共通で、どこが違うか整理して
```

**確認したいポイント**:

- `createXxxCommand()` 関数がファクトリとして依存を受け取る構造
- `repository.findById()` → ドメイン関数 → `repository.save()` → `eventBus.publish()` の4ステップ
- `err({ type: 'SomeError' })` の Discriminated Union パターン

### Step 3：CLAUDE.md と規約からプロジェクトのルールを把握する（10分）

```text
@CLAUDE.md @docs/conventions/ を読んで、このプロジェクトのコーディング規約の中で
「これを知らずに実装すると間違えやすいポイント」を3つ挙げて
```

（時間を置く。分からない用語はさらに調べる。）

### Step 4：ペアシェア（5分）

> **「AIの解説を聞いて、一番印象に残ったパターンは何ですか？」**

- AIに聞いてみて「わかりやすかった」「ここはもっと深掘りしたい」と感じた点
- 「自分で読むより速いか、遅いか？」 — AIにコード解説を頼む体験の率直な感想

---

## 10:45 - 11:30 実践①：Claude Code CLIでチケットベース開発を体感する（45分）

### Claude Code CLI とは

コマンドラインから Claude Code エージェントを呼び出すCLIツール。
CLAUDE.md や docs/conventions/ を参照しながら、ターミナル上でエージェントと対話する。

### 今回の流れ

```text
Step 1: 事前準備              環境を確認する
Step 2: プランモードで計画     エージェントが既存コードを調査し、実装計画を提示
         ↓ 人間が計画を確認・承認
Step 3: 実装                  計画に従ってコード・テストを生成
         ↓ 実装結果を確認
Step 4: 検証                  品質ゲート（test / lint / tsc）を通す
```

### ハンズオン：主催者がイベントを公開する（PublishEvent）をプランモードで実装

**今日実装するシナリオ:**

`CreateEvent` でイベントが DRAFT 状態で作成される。次のステップとして、主催者が DRAFT イベントを PUBLISHED に遷移させ、参加申込を受け付け可能にする API を実装する。

**Step 1: 事前確認**

```bash
cd meetup-sample-v2
./scripts/docker-dev.sh up
./scripts/docker-dev.sh install   # 依存関係インストール
./scripts/docker-dev.sh test      # テストが通ることを確認（全グリーンを確認）
```

作業ブランチを作成する：

```bash
git checkout -b feature/publish-event-cli
```
備考： ブランチをAIに作らせることもできる。

**Step 2: プランモードで計画を立てる**

Claude Code を起動する：

```bash
claude
```

すでに起動中であればセッションを新しくする（`/clear` コマンドを入力するか、ターミナルを新しく開いて `claude` を実行する）。

起動後、**Shift+Tab** を押してプランモードに切り替える（画面左下に `plan` と表示されたことを確認）。

プランモードで以下を入力する：

```text
次のbackendの実装計画を立てて。疑問点があれば質問して。

## 主催者がイベントを公開する（PublishEvent）

概要：主催者が DRAFT 状態のイベントを PUBLISHED に遷移させ、
      参加申込を受付可能にする。
      event コンテキスト（backend/src/event/）に実装する。

受け入れ条件
- [ ] `status=DRAFT` のときのみ公開できる
- [ ] すでに公開済み（PUBLISHED / CLOSED / CANCELLED）の場合 `EventAlreadyPublishedError` を返す
- [ ] 公開後は `status=PUBLISHED` に遷移する
- [ ] `EventPublished` が発火される

参考
- 同じ event コンテキストの create-event.command.ts が実装パターンの参考になる
- ステータス遷移は community コンテキストの approve-member.command.ts が参考になる
```

エージェントが既存コードを調査し、実装計画を提示する。
計画を確認し、問題なければ承認して次へ進む。

> **ポイント**: プランモード中はエージェントがファイルを変更しない。計画に納得してから承認することで、想定外の変更を防げる。

**Step 3: 実装**

計画を承認すると、エージェントがコード・テストを生成する。

観察ポイント：

- 新しいファイルはどこに配置されたか？（`event/usecases/commands/publish-event.command.ts` など）
- エラー型はどこに追加されたか？（`event/errors/event-errors.ts`）
- `composition.ts` の依存注入が更新されているか？
- 受け入れ条件がテストでカバーされているか？

**Step 4: 検証（品質ゲート）**

改めて、以下を実行して品質ゲートを通す
（AIエージェントが自動で実行するはずだが改めて）：

```bash
./scripts/docker-dev.sh bash -c "cd backend && npm run test:coverage"
./scripts/docker-dev.sh bash -c "cd backend && npm run review"
```

**Step 5: 成果をコミットする**

品質ゲートが通ったら、作業をコミットしておく：

```bash
git add -A
git commit -m "feat: イベント公開API実装 (Claude Code CLI)"
```

もしくは Claude Code にコミットさせる：

```text
git add して日本語のコミットメッセージでコミットして
```

> **ポイント**: コミットしておくことで、午後のラウンド1・ラウンド2での比較ベースラインになる。

### うまくいかないときのヒント

```text
# エラーが出たら
「エラーが出た。原因を特定し修正して。[エラーメッセージを貼り付け]」

# Command の実装パターンを確認したいとき
「backend/src/event/usecases/commands/create-event.command.ts と
 backend/src/community/usecases/commands/approve-member.command.ts を読んで、
 PublishEvent コマンドを実装する場合の構造を教えて」

# テストが書けないとき
「backend/src/event/usecases/commands/__tests__/create-event.command.test.ts を読んで、
 PublishEvent のテストを書く場合のモック構成と正常系・異常系のパターンを教えて」
```

---

## 11:30 - 12:00 学びの整理①（30分）

### ペアシェア（10分）

以下の問いをペアで話す：

> **「エージェントに仕事を渡してみて、想像と違ったことは何でしたか？」**

- AIからの計画の提示を見て、どう感じたか？
- AIの成果物レビューで、どう感じたか？
- 自分で書くのと比べて、何が変わったか？
- 「どこをAIにお任せ」し、「ここは自分が判断しないといけない」と感じたポイントは？

### 全体共有（10分）

各ペアから1つずつ気づきを共有する。

### 振り返りメモ（10分）

個人で以下をメモする：

| | 内容 |
|--|------|
| **気づき** | Claude Code CLIを使ってみて気づいたこと |
| **新たに学びたいこと** | まだ分からず、もっともっと知りたいこと |

---

## 13:00 - 13:15 ワーク導入：3つの勘所と進め方（15分）

### この午後で体感すること

AI駆動開発を実践で回すときの**3つの勘所**を、2ラウンドの試行錯誤で体に染み込ませる。

```text
勘所① 計画レビュー
  エージェントが出した計画を、受け入れ条件・参考コード・規約の3点で評価する

勘所② コードレビュー
  生成コードと期待値のずれを言語化する（規約違反・テスト漏れ・ドメイン理解）

勘所③ ずれを理解してハーネスを整備する
  「なぜそのずれが生まれたか」を仮説立て、CLAUDE.md / docs/conventions / .claude/hooks に反映する
```

この3つを**2ラウンド**回す：

```text
ラウンド1 → ずれを発見・ハーネス改善案を作る
ラウンド2 → 改善したハーネスで再実行し、差を確認する
```

### 発表テンプレート（最初に受け取る）

午後終わりに**ペア4分**で発表する。以下の枠に書き込むだけでよい：

```text
【ペア名】___________

ずれ：
  ラウンド1で気づいたAIの出力と期待値の差

仮説：
  なぜそのずれが生まれたか

ハーネス改善：
  何を追加・修正したか（CLAUDE.md / conventions / hooks のどれ？）

再実行結果：
  ラウンド2でずれは解消したか、新たな発見は何か
```

### ペア編成と準備（5分）

1. ペアを決める（ファシリテーターがアサイン）
2. 作業ブランチのベースを確認する：

```bash
cd meetup-sample-v2
git checkout main
git log --oneline -3   # 実践①（feature/publish-event-cli）コミットを確認
```

---

## 13:15 - 14:15 ラウンド1：1周目のサイクルを回す（60分）

### ラウンド1 の目的

**意図的に弱めたハーネス**で PublishEvent を実装させ、3つの勘所を体感する。

「ハーネスを全部剥がす」のではなく、**一部を外す**ことで「何がなくなるとずれが生じるか」を観察しやすくする。

### Step 1：実験用ブランチを切る（5分）

```bash
git checkout -b experiment/round-1
```

ハーネスの一部を外す（コミットしておく）：

```bash
# テスト規約を削除（テスト命名・構造のブレが生まれやすくなる）
rm -f docs/conventions/testing.md

# .claude/hooks を削除（品質ゲートが自動実行されなくなる）
rm -rf .claude/hooks/

# 削除をコミット
git add -A
git commit -m "chore: ラウンド1用にハーネス一部を削除"
```

さらに AGENTS.md の `## Quality Gates` セクションを削除する（CLAUDE.md は `@AGENTS.md` でインポートしているため AGENTS.md だけ修正すれば両方から消える）：

```text
（Claude Code に依頼）
「AGENTS.md の '## Quality Gates' セクション全体を削除して」
git add AGENTS.md
git commit -m "chore: Quality Gates セクションを削除（ラウンド1実験用）"
```

> **ポイント**: CLAUDE.md・AGENTS.md・coding.md・既存ソースコードは**残っている**。
> 消えたのは「テスト命名規約」「自動品質チェック（hooks）」「品質チェックコマンドの明示（Quality Gates）」の3つ。
> hooks なし＋Quality Gates 言及なし → エージェントが品質チェックをスキップするかどうかを観察する。

### Step 2：計画レビュー（15分）

新しいセッションで Claude Code を起動する（`/clear` またはターミナルを新しく開く）：

```bash
claude
```

**Shift+Tab** でプランモードに切り替えてから以下を入力する：

```text
次の実装計画を立てて。疑問点があれば質問して。

## 主催者がイベントを公開する（PublishEvent）

概要：主催者が DRAFT 状態のイベントを PUBLISHED に遷移させ、
      参加申込を受付可能にする。
      event コンテキスト（backend/src/event/）に実装する。

受け入れ条件
- [ ] `status=DRAFT` のときのみ公開できる
- [ ] すでに公開済み（PUBLISHED / CLOSED / CANCELLED）の場合 `EventAlreadyPublishedError` を返す
- [ ] 公開後は `status=PUBLISHED` に遷移する
- [ ] `EventPublished` が発火される

参考
- 同じ event コンテキストの create-event.command.ts が実装パターンの参考になる
- ステータス遷移は community コンテキストの approve-member.command.ts が参考になる
```

計画が出たら、**プランモードのまま**ペアで以下を評価する（承認前に止める）：

| 評価ポイント | チェック |
| --- | --- |
| 受け入れ条件の4つが計画に含まれているか | ✓ / ✗ |
| 既存の command パターン（4ステップ）を踏まえているか | ✓ / ✗ |
| テストファイルの配置・命名について言及があるか | ✓ / ✗ |
| `EventPublished` 発火の実装箇所が明示されているか | ✓ / ✗ |

気になる点があればプランモード内で質問・修正を依頼してから承認する。

> **勘所①の体感**: 「計画を承認する前に何を確認すべきか」が感覚的にわかってきたら成功。

### Step 3：実装とコードレビュー（25分）

計画を承認して実装させる。実装完了後、ペアでコードをレビューする：

```bash
# 手動で品質ゲートを実行（AI hook がないため実装中のリアルタイムチェックが止まる。git コミット時の lefthook は引き続き動作する）
./scripts/docker-dev.sh bash -c "cd backend && npm run test:coverage"
./scripts/docker-dev.sh bash -c "cd backend && npm run review"
```

実装をコミットする：

```bash
git add -A
git commit -m "feat: PublishEvent ラウンド1実装"
```

**観察ポイント：ハーネスを弱めると何が変わるか？**

| 観点 | 期待値（実践①・ハーネス完全版） | ラウンド1で何が起きたか |
| --- | --- | --- |
| テスト命名 | `describe`/`it` が日本語 | 英語になった？ |
| テスト構造 | `buildXxx()` ヘルパー・`__tests__/` 配置 | 構造が変わった？ |
| 実装中フィードバック | Edit/Write のたびに ESLint が即時実行（lint-guard） | ESLint エラーが実装中に検出されなかった？ |
| テスト実行タイミング | hooks なし → エージェントが自発的にテストを回したか | スキップされた？ 手動実行が必要だった？ |
| エラーハンドリング | Result 型 + Discriminated Union | 既存コードを真似たか？ |

備考：AIエージェントは既存コードを読んで推測するするため、ハーネスを弱めても完全に無視するわけではありません

### Step 4：ずれを抽出してハーネス改善案を書く（15分）

ペアで以下を議論し、**発表テンプレートの「ずれ」「仮説」「ハーネス改善」を書き込む**。

> **「期待値と違った部分はどこ？　それはなぜ生まれた？」**

改善案を実際にハーネスへ反映する：

```bash
# 例1：testing.md にテスト命名ルールを追記（または復活）
# 例2：.claude/hooks/ を復活させる
# 例3：CLAUDE.md に「テスト構造」に関する追記を入れる
```

```text
（Claude Code に聞いてみる）
「さっきの実装でテスト命名が英語になっていた。
 これが日本語になるよう CLAUDE.md か docs/conventions/testing.md に何を書けばよいか教えて」
```

> **勘所③の体感**: 「ずれの原因を突き止め、ハーネスに書けば次回に改善される」という手応えを得られたら成功。

---

## 14:30 - 15:30 ラウンド2：改善したハーネスで再実行する（60分）

### ラウンド2 の目的

ラウンド1で改善したハーネスで**もう一度同じシナリオを実行**し、改善の効果を確認する。

### Step 1：ラウンド2用ブランチを切る（5分）

```bash
git checkout main
git checkout -b experiment/round-2
```

ラウンド1で作った改善案をここで適用する（ファイルを追記・修正する）。

> **ポイント**: ラウンド2の起点は `main`（ハーネス完全版）。`experiment/round-1` からは始めない。ラウンド1の削除を引き継がず、改善内容だけを反映させる。

### Step 2：計画レビュー → 実装（40分）

新しいセッションで Claude Code を起動し、同じプロンプトを入力する：

```bash
claude
```

プランモード（Shift+Tab）で、ラウンド1と**同じプロンプト**を入力する。

計画が出たら同じ評価ポイントでレビューし、承認 → 実装 → 品質ゲート：

```bash
./scripts/docker-dev.sh bash -c "cd backend && npm run test:coverage"
./scripts/docker-dev.sh bash -c "cd backend && npm run review"
git add -A
git commit -m "feat: PublishEvent ラウンド2実装"
```

### Step 3：改善前後を比較する（20分）

`git diff` でラウンド1とラウンド2の AI 出力を比較する：

```bash
git diff experiment/round-1 experiment/round-2 -- backend/src/event/
```

または AI に比較を頼む：

```text
experiment/round-1 と experiment/round-2 を比較して。
backend/src/event/ 内のコードについて、以下の観点で違いを分析して：
- テストの命名と構造
- エラーハンドリングのパターン
- 規約への準拠度
```

**ラウンド2の観察で確認すること**：

- ラウンド1で書いた改善案は効いたか？（ずれが解消したか）
- 改善したが、別のずれが出てきた部分はないか？
- まだ残っている改善余地は何か？

> **勘所③の完成**: 「ハーネスを育てるとAIの出力が変わる」という体験と「まだ残っている課題」の両方を言語化できれば成功。

---

## 15:30 - 15:45 発表準備（15分）

受け取った発表テンプレートを完成させる：

```text
【ペア名】___________

ずれ：
  ラウンド1で気づいたAIの出力と期待値の差

仮説：
  なぜそのずれが生まれたか

ハーネス改善：
  何を追加・修正したか（CLAUDE.md / conventions / hooks のどれ？）

再実行結果：
  ラウンド2でずれは解消したか、新たな発見は何か
```

4分で発表できる分量に絞る。「ずれ」は1〜2つに絞り込む。

---

## 16:00 - 16:30 発表セッション：ペアごとに4分で発表（30分）

### 発表の進め方

- 各ペア4分（司会が時間切れで次へ）
- 発表テンプレートの順に話す：ずれ → 仮説 → 改善 → 再実行結果
- 聴衆は「自分ならどう改善する？」の観点でメモを取る

### 発表後の問いかけ（全体）

各ペアの発表を聞いて、気づいたことをファシリテーターが拾いながら整理する：

- **共通して見られたずれは何か？**
- **最も効果的だったハーネス改善はどれか？**
- **「既存コードから推測できた」vs「明示的に書かないと伝わらない」ルールの境界は？**

---

## 16:30 - 17:30 全体ふりかえり：これからの開発の未来を語り合う（60分）

### 今回用意したハーネスの種明かし（10分）

今日使ったハーネス（CLAUDE.md・docs/conventions/・.claude/hooks/・品質ゲート等）は、この研修のために私が用意したもの。

```text
meetup-sample-v2/
├── CLAUDE.md                      # プロジェクトルール（Claude Code 固有設定）
├── AGENTS.md                      # エージェント共通ルール（技術スタック・規約）
├── docs/
│   ├── conventions/               # コード品質ガイドライン
│   │   ├── coding.md              # コーディング規約・実装パターン
│   │   └── testing.md             # テスト方針・命名規則
│   └── decision-logs/             # 意思決定記録（ADR）
├── .claude/
│   ├── hooks/                     # 自動品質チェック（PostToolUse / PreToolUse フック）
│   └── skills/                    # 日本語コミット・ADR記録 等
└── 品質ゲート（test / lint / tsc / 循環依存チェック）
```

ポイント：

- ハーネスは自分たちのプロジェクトに合わせて**自分たちで組み立てる**もの
- Claude Code と対話しながら育てていくこともできる
- 一度作って終わりではなく、プロジェクトとともに進化させるもの
- セッションを Claude Code とふりかえりながら、適宜整備を続けるのがコツ

### これからの開発の未来を語り合う（40分）

以下のテーマから、興味のあるものを選んでグループで議論する：

> **テーマ1：「開発者の役割はどう変わるか？」**
>
> - 「設計」「レビュー」「ドメイン理解」の比重はどう変わるか？
> - AIエージェントと人間の役割分担はどうなるか？

> **テーマ2：「チーム開発はどう変わるか？」**
>
> - 開発者はAIエージェントの監督役になると言われるが、具体的にはどんなスキルが必要になるか？
> - エージェントが書いたコードのレビューは、人が書いたコードと同じでいいか？
>
> **テーマ3：「自分のプロジェクトで、明日から何を変えるか？」**
>
> - 今日の体験を踏まえて、最初の一歩は何か？
> - 1週間後・1ヶ月後に実現したい状態は？

---

## 17:30 - 17:45 クロージング（15分）

### 3行コミットメント ＆ 感想（15分）

以下を書いて、隣の人に宣言する（声に出すことで定着する）：

```text
今日学んだこと：
_____________________________

明日から試すこと（具体的に1つ）：
_____________________________

1週間後のゴール：
_____________________________
```

```text
今日の感想(宣伝のためWebサイトに掲載させてください。名前なしでも大丈夫です)

_____________________________

_____________________________
```

---

## ファシリテーターメモ

### 今日実装するシナリオのまとめ

| 場面 | シナリオ | 参照コード |
| --- | --- | --- |
| コードリーディング | `CreateEvent`（実装済み） | `backend/src/event/usecases/commands/create-event.command.ts` — Command パターンの理解に使う |
| コードリーディング | `ApproveMember`（実装済み） | `backend/src/community/usecases/commands/approve-member.command.ts` — ステータス遷移の参考に使う |
| 実践①・ラウンド1・ラウンド2 | `PublishEvent`（未実装） | `backend/src/event/usecases/commands/publish-event.command.ts`（新規作成） |

### 時間が余ったとき

- 参加者の「自分のプロジェクトの CLAUDE.md」をライブ作成する
- `CancelEvent`（PublishEvent の次のシナリオ）に挑戦する
- ラウンド2のハーネス改善をさらに深掘りする

### つまずきやすいポイント

- **セットアップ**: `./scripts/docker-dev.sh install` 後に `./scripts/docker-dev.sh test` が通ることを事前確認しておく
- **gh CLI 認証**: `gh auth status` で事前確認
- **プランモードの切り替え忘れ**: Shift+Tab でモードが変わることを最初にデモする
- **composition.ts の更新忘れ**: Command を実装しても `backend/src/event/composition.ts` に依存注入を追加しないと動かない。エージェントが見落とした場合は「composition.ts も更新して」と伝える
- **エージェントが止まる**: エラーメッセージをそのままエージェントに渡すよう促す
- **ブランチ取り違え**: ラウンド2の起点は必ず `main`。`experiment/round-1` から始めないよう声がけする
- **改善案の反映漏れ**: ラウンド2のブランチを切った後、ハーネス改善内容を適用してから Claude Code を起動するよう確認する

### 研修後のフォローアップ

- 1週間後に「Try できたこと」をSlackで共有してもらう
- CLAUDE.md のレビュー会を別途設定することを推奨
